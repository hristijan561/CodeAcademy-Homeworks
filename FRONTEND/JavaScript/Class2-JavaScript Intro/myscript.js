// Write a JavaScript program that will calculate the area of a circle

function calculateAreaOfCircle(pi, a) { 
    var result;
    result = pi * (a * a);
    console.log(result);
    return result;

}

calculateAreaOfCircle(3.1415, 5 );
